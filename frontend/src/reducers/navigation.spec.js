import { expect } from 'chai';
import enzyme from 'enzyme';
import React from 'react';

import navigation from './navigation';

describe('navigation reducer', () => {
  it('handles ADD_NEW_POST action', () => {
    fail();
  });

  it('handles CHANGE_CATEGORY action', () => {
    fail();
  });
});
